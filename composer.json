{
    "name": "auth/jwt",
    "description": "Proyecto para autenticación con JWT",
    "type": "project",
    "require": {
        "firebase/php-jwt": "^6.0",
        "php": "^8.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "test": "phpunit"
    }
}
